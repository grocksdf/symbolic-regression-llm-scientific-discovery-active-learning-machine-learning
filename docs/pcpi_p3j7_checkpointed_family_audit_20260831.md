# P3J.7 checkpointed complete-family audit (2026-08-31)

P3J.7 composes the four frozen likelihood-power models into each adaptive
quadrature look. Every model finishes its identity-bound fine/coarse grid
before the lower-envelope certificate is evaluated. A later look receives the
matching complete preceding estimate for each model; completed model grids are
reloaded after interruption rather than recomputed.

The checkpointed operational entry point scores the complete family before
candidate selection. An interruption in any model propagates before selection,
and no response, validation, held-out, random-retry, or alternate-utility
surface exists in this composition.

This Gate does not authorize real execution. P3J.8 must bind this ranking root
to dataset, seed, query, candidate-domain and source identities in a supervised
formal runner, with terminal failure and progress reporting.
