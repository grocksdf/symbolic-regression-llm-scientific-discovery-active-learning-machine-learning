# P3H.4 likelihood-power residual-family correctness result

## Outcome

The frozen P3H.4 Gate passed all nine registered decisions on source commit
`c702d408fad9dc87c5ea5495958d9a63ce87d5df` and source tree
`2f388546ea0d6e0c1c57fbad8a98ac501560b322`.

The state mapping is now frozen as
`model-specific-likelihood-power-prequential-raw-pit-family-v1`, with history
order `initial-development-stored-order-then-acquisition-reveal-order` and
binding `exact-engine-posterior-residual-law-object-binding`.

This is a deterministic state-mapping correctness result. It is not a real or
simulated experiment and does not authorize candidate selection.

## Correctness audit

The fixture reconstructed powers `[0.25, 0.5, 1.0]` from the same 12-position
chronological opened history. Every target hash and raw-PIT sequence was
distinct. The minimum pairwise maximum PIT-sequence distance was
`0.04293827817539586`; the maximum was `0.10218068411467185`. Thus the
target-specific state distinction is substantive, not only a label change.

Reconstruction was bitwise repeatable. Reversing the chronological history
changed the history commitment and residual-family hash, while the maximum
difference between the corresponding final conjugate batch posterior
probabilities was only `8.604228440844963e-16`, below the frozen `3e-14`
tolerance. This confirms that the residual state correctly retains
prequential-order information that the exchangeable batch posterior does not.

An issued eight-observation prefix remained unchanged after a separate
reconstruction was given altered later values. The acquisition bundle had no
raw history action or response attributes. Production binding accepted the
exact family engine/posterior objects and rejected separately refitted,
numerically equivalent posterior objects.

The family stable hash was
`067df5fd0e8000caca05b9effe035e9a06e351868c18770c0c262c726b0498c3`.
The chronological-history commitment was
`56ce15ee8c75159769707850267772658268a2654d07c0cf3d12729adad3dcb7`.

## Integrity and regression

The production source SHA-256 was
`585faff1e9eec58c6f83defd6c735154c5972e9fd7be1f7ccda363a94c83f977`.
The runtime dependency hash was
`b7bf88a64dd375e25c7d679de129c654c3346462215ffa24c425c762fb8bc4a6`,
identical to the frozen P3G.5/P3H.2--P3H.3 environment.

The archived output directory is
`outputs/p3h_4_likelihood_power_residual_family_correctness_c702d408_20260826`.
Its `summary.json` SHA-256 is
`d5203cc23c7aa31485bde0fb52f10d8e2bab96534249788f598403afd096bd9d`, exactly
matching `manifest.json`.

The combined P3H.3/P3H.4 targeted suite passed 22/22. The broader P3H,
acquisition and discrepancy regression completed with exit code zero. The full
repository collected and passed 683/683 tests across 87 test files. No failure
was masked, skipped or relaxed.

## Access and claim boundary

The Gate records:

- `p3h2_eta1_calibration_transferred_to_family: false`;
- `family_wide_calibration_established: false`;
- `simulated_experiment: false`;
- `real_data_access: false`;
- `validation_response_access: false`;
- `candidate_response_access: false`;
- `candidate_selection_executed: false`;
- `operational_execution_authorized: false`;
- `heldout_access: false`;
- `formal_efficacy_evidence: false`.

A pass freezes only the one-to-one state construction, chronological leakage
boundary, raw-history erasure and exact production binding. It does not prove
calibration for any likelihood power, transfer P3H.2 compatibility to this
family, establish acquisition efficacy, or support a paper result.

The next admissible phase is a separately frozen family-wide calibration-only
Gate on real opened calibration sequences. Candidate responses, acquisition
execution and held-out data must remain closed, and the user alone executes its
unique command after the environment and source commit are frozen.
