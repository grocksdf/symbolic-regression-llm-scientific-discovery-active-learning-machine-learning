# PCPI P3H.9 interval-resolved real-development protocol

P3H.9 is the first real protocol authorized to use the P3H.8 total interval
resolver. It is explicitly failure-informed development after incomplete
P3H.7, not independent confirmation.

All experimental coordinates remain frozen: the three registered real targets,
eight seeds, split seed `20260807`, 32 initial observations divided into 16 base
warmup and 16 residual-training observations, 32 acquired responses, 128
candidate rows and 256 validation rows. Random, uncertainty, QBC and PCPI retain
identical row commitments and response budgets. Held-out remains closed.

PCPI retains the complete likelihood-power family `(0.125, 0.25, 0.5, 1.0)`,
model-specific leakage-safe prequential residual states and the transformed
semiparametric maximin joint utility. The only protocol change is the declared
unresolved action
`interval-possible-maximizer-representative-mmd-then-candidate-id-v1`.

Protocol validity requires 96 terminal policy runs, complete response-budget
accounting, continuous P3H family-hash chains, no P3H state on baselines, closed
held-out, no forbidden fallback, every selected candidate inside its recorded
admissible set and a 100% total PCPI decision-valid rate. Primary-ranking rate
and secondary-resolution rate are reported separately and cannot be conflated.

The frozen runtime dependency hash remains
`b7bf88a64dd375e25c7d679de129c654c3346462215ffa24c425c762fb8bc4a6`.
Strong, mixed, negative and failed outcomes are retained. Even a valid positive
result is failure-informed real-development evidence only and cannot establish
universal superiority, untouched confirmation, physical intervention, a new
scientific law or AISTATS acceptance.
